$("#submit").click(function( event ) {
  $('#filter-select').val("select-filter")
  $('#country-select').val("select-country")
  $('#channel-id').val("")
  alert( "Segment Filter is Saved.The Segments will be used for filtering the user and pushing it to the channel" );

});